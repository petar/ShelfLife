
Build steps
===========

(*) Set the SHELFLIFE environment variable to point at this directory

(*) Make sure the MongoDB bin directory is in the PATH environment variable

(*) Install COMPASS (compass-style.org)

	gem update --system
	gem install compass

